<?php

declare(strict_types=1);

namespace UniversalPackages\CountryService;

/**
 * Locale enum derived from existing files in data/ directory.
 */
enum DataLocale: string
{
    /*
     * 保加利亚语（保加利亚）
     */
    case BG_BG = 'BG_BG';

    /*
     * 捷克语（捷克）
     */
    case CS_CZ = 'CS_CZ';

    /*
     * 丹麦语（丹麦）
     */
    case DA_DK = 'DA_DK';

    /*
     * 德语（德国）
     */
    case DE_DE = 'DE_DE';

    /*
     * 希腊语（希腊）
     */
    case EL_GR = 'EL_GR';

    /*
     * 英语（美国）
     */
    case EN_US = 'EN_US';

    /*
     * 西班牙语（西班牙）
     */
    case ES_ES = 'ES_ES';

    /*
     * 芬兰语（芬兰）
     */
    case FI_FI = 'FI_FI';

    /*
     * 法语（法国）
     */
    case FR_FR = 'FR_FR';

    /*
     * 印度尼西亚语（印度尼西亚）
     */
    case ID_ID = 'ID_ID';

    /*
     * 意大利语（意大利）
     */
    case IT_IT = 'IT_IT';

    /*
     * 日语（日本）
     */
    case JA_JP = 'JA_JP';

    /*
     * 韩语（韩国）
     */
    case KO_KR = 'KO_KR';

    /*
     * 立陶宛语（立陶宛）
     */
    case LT_LT = 'LT_LT';

    /*
     * 马来语（马来西亚）
     */
    case MS_MY = 'MS_MY';

    /*
     * 书面挪威语（挪威）
     */
    case NB_NO = 'NB_NO';

    /*
     * 荷兰语（荷兰）
     */
    case NL_NL = 'NL_NL';

    /*
     * 波兰语（波兰）
     */
    case PL_PL = 'PL_PL';

    /*
     * 葡萄牙语（巴西）
     */
    case PT_BR = 'PT_BR';

    /*
     * 葡萄牙语（葡萄牙）
     */
    case PT_PT = 'PT_PT';

    /*
     * 罗马尼亚语（罗马尼亚）
     */
    case RO_RO = 'RO_RO';

    /*
     * 俄语（俄罗斯）
     */
    case RU_RU = 'RU_RU';

    /*
     * 斯洛文尼亚语（斯洛文尼亚）
     */
    case SL_SI = 'SL_SI';

    /*
     * 瑞典语（瑞典）
     */
    case SV_SE = 'SV_SE';

    /*
     * 泰语（泰国）
     */
    case TH_TH = 'TH_TH';

    /*
     * 土耳其语（土耳其）
     */
    case TR_TR = 'TR_TR';

    /*
     * 越南语（越南）
     */
    case VI_VN = 'VI_VN';

    /*
     * 简体中文（中国大陆）
     */
    case ZH_CN = 'ZH_CN';

    /*
     * 繁体中文（中国台湾）
     */
    case ZH_TW = 'ZH_TW';

}
